cd into `tts-gan` directory and run:

```
..\_pyenv_ttsgan\Scripts\python.exe .\bench_train.py
```

where,
`..\_pyenv_ttsgan\Scripts\python.exe` is the path to where I have setup python environment for tts-gan.
